# ---- Core ML (all)
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
scipy>=1.12.0
matplotlib>=3.8.0
seaborn>=0.13.0

# ---- Hugging Face (all)
transformers>=4.37.0
datasets>=2.17.0
evaluate>=0.4.0
huggingface-hub>=0.20.0
accelerate>=0.27.0

# ---- Web/API (all)
gradio>=4.16.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# ---- Thai
pythainlp>=5.0.0

# ---- Utilities
tqdm>=4.66.0
lime>=0.2.0
optimum[onnxruntime]>=1.16.0

# ---- PyTorch (pick per platform automatically)
# macOS (CPU/MPS)
torch==2.4.*; platform_system == "Darwin"
torchvision==0.19.*; platform_system == "Darwin"
torchaudio==2.4.*; platform_system == "Darwin"

# Linux AMD ROCm (pip wheels)
--find-links https://download.pytorch.org/whl/rocm6.2
torch==2.4.*; platform_system == "Linux" and platform_machine == "x86_64"
torchvision==0.19.*; platform_system == "Linux" and platform_machine == "x86_64"
torchaudio==2.4.*; platform_system == "Linux" and platform_machine == "x86_64"
